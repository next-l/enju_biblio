<div class="row">
<div class="col-md-9">
<h1><%= t('page.showing', model: t('activerecord.models.series_statement_merge')) -%></h1>
<div class="card-body bg-light">
<p id="notice"><%= notice %></p>

<p>
  <strong><%= t('activerecord.models.series_statement') -%>:</strong>
  <%= link_to @series_statement_merge.series_statement.original_title, @series_statement_merge.series_statement -%>
</p>

<p>
  <strong><%= t('activerecord.models.series_statement_merge_list') -%>:</strong>
  <%= link_to @series_statement_merge.series_statement_merge_list.title, @series_statement_merge.series_statement_merge_list -%>
</p>

</div>
</div>

<div class="col">
  <ul>
    <li><%= link_to t('page.add', model: t('activerecord.models.series_statement')), series_statement_merge_list_series_statements_path(@series_statement_merge.series_statement_merge_list) -%></li>
    <li><%= link_to t('page.edit'), edit_series_statement_merge_path(@series_statement_merge) -%></li>
    <li><%= back_to_index(flash[:page_info]) -%></li>
  </ul>
</div>
